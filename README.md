# Analysis-of-metro-station-locations
Use `python -u "C:folderpath\Main.py"` in terminal to start website.
Download VSCode and Code Runner extension and launch Main.py as an alternative.

Use http://127.0.0.1:5000/ to visit website.

README.md will be expended
